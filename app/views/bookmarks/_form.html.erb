<%= simple_form_for [ @list, @bookmark ] do |b| %>
  <%= b.input :comment %>
  <%= b.association :movie, collection: Movie.all.order(:title), prompt: 'Select a movie', as: :radio_buttons %>
  <%= b.button :submit, "Añadir este marcador", class: "btn btn-primary" %>
<% end %>
